// Public exports for general info display components
export { default as GeneralInfoDisplay } from './general-info-display';

// Internal components (not exported as they're only used within the general-info-display directory)
// - None currently, but this structure allows for future expansion
