// Public exports for expenses display components
export { default as ExpensesDisplay } from './expenses-display';

// Internal components (not exported as they're only used within the expenses-display directory)
// - None currently, but this structure allows for future expansion
