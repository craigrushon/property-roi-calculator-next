// Public exports for incomes display components
export { default as IncomesDisplay } from './incomes-display';

// Internal components (not exported as they're only used within the incomes-display directory)
// - None currently, but this structure allows for future expansion
