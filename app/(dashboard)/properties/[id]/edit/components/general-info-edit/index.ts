// Public exports for general info edit components
export { default as GeneralInfoEdit } from './general-info-edit';

// Internal components (not exported as they're only used within the general-info-edit directory)
// - None currently, but this structure allows for future expansion
